# Optional. Without these, Let's get this printed still saves the picture
# and opens a mail draft with shop suggestions.
RESEND_API_KEY=
RESEND_FROM=Wild Kit <hello@your-domain.com>

# Pay lives on /pay via Stripe Payment Links. No secret needed for those links.
# Add a restricted key later if we switch the desk to Checkout Sessions.
# STRIPE_SECRET_KEY=
