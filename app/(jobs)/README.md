# Job prototypes — not the live site

`next.config.ts` redirects these paths to website briefs. Keep them as the design reference for the native apps.

Serious product work: `apps/lemonade`.

Read `docs/APPS.md`.
